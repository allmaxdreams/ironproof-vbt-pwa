# 🚀 Інструкція з розгортання VBT Tracker на Netlify

Оскільки наш додаток використовує **Web Bluetooth API**, для його роботи на реальних пристроях (смартфонах) необхідне безпечне з'єднання **HTTPS**. Локальний сервер (`http://localhost`) підтримує Bluetooth лише для розробки на комп'ютері.

Найшвидший і найпростіший спосіб безкоштовно отримати HTTPS і розмістити наш PWA в інтернеті — це сервіс **Netlify**.

Нижче наведено 3 різні способи розгортання вашого проєкту. Оберіть той, який вам найбільше підходить.

---

## Спосіб 1: Найпростіший (Drag & Drop) 🖱️

Цей спосіб не потребує Git чи командного рядка. Ідеально підходить для швидкого тестування прототипу.

1. **Зберіть проєкт:**
   Відкрийте термінал у папці вашого проєкту (`/Users/allmaxdreams/Documents/IronProof/vbt-tracker-pwa`) і виконайте команду:
   ```bash
   npm run build
   ```
   *Це створить папку `dist` з оптимізованими файлами вашого додатка.*

2. **Завантажте на Netlify:**
   - Зареєструйтесь або увійдіть у свій акаунт на [Netlify](https://app.netlify.com/).
   - Перейдіть на вкладку **Sites** (Сайти).
   - Внизу сторінки ви побачите блок *"Drag and drop your site output folder here"*.
   - Відкрийте Finder на вашому Mac, знайдіть папку `vbt-tracker-pwa`, всередині неї знайдіть щойно створену папку **`dist`**.
   - **Перетягніть папку `dist`** у цей блок на сторінці Netlify.

3. **Сайт готовий!** Netlify миттєво згенерує для вас посилання (щось на зразок `https://clever-name-123.netlify.app`). Ви можете змінити цю назву в налаштуваннях сайту (Site Settings -> Domain management -> Options -> Edit site name).

---

## Спосіб 2: Через GitHub (Рекомендовано для подальшої розробки) 🐙

Цей спосіб найкращий, якщо ви плануєте далі розвивати проєкт. Будь-які зміни, пушнуті в GitHub, будуть автоматично оновлюватися на сайті.

1. **Створіть репозиторій на GitHub** (наприклад, `ironproof-vbt-pwa`) і завантажте туди ваш код:
   ```bash
   cd /Users/allmaxdreams/Documents/IronProof/vbt-tracker-pwa
   git init
   git add .
   git commit -m "Initial commit MVP"
   git branch -M main
   git remote add origin https://github.com/ВАШ_АККАУНТ/ironproof-vbt-pwa.git
   git push -u origin main
   ```

2. **Підключіть Netlify:**
   - В панелі [Netlify](https://app.netlify.com/) натисніть **"Add new site"** -> **"Import an existing project"**.
   - Виберіть **GitHub** і авторизуйтесь.
   - Оберіть ваш репозиторій `ironproof-vbt-pwa`.
   - Налаштування збірки (Build settings) Netlify має визначити автоматично:
     - *Build command:* `npm run build`
     - *Publish directory:* `dist`
   - Натисніть **"Deploy site"**.

---

## Спосіб 3: Через Netlify CLI (Для терміналу) 💻

Якщо ви любите робити все не виходячи з терміналу:

1. **Встановіть Netlify CLI глобально:**
   ```bash
   npm install netlify-cli -g
   ```

2. **Авторизуйтесь:**
   ```bash
   netlify login
   ```
   *(Відкриється браузер для підтвердження)*

3. **Розгорніть проєкт:**
   Переконайтеся, що ви в папці `vbt-tracker-pwa`, і виконайте:
   ```bash
   netlify deploy
   ```
   - На питання "What would you like to do?" оберіть **"Create & configure a new site"**.
   - Оберіть вашу команду (акаунт).
   - Придумайте назву сайту (або залиште порожнім для випадкової).
   - Поле *Publish directory* вкажіть як **`dist`**.

   *(Це створить чорнову версію (Draft). Щоб відправити у продакшн, виконайте: `netlify deploy --prod`)*

---

## 📱 Як тестувати на смартфоні (PWA)

Після того, як ви отримали посилання на ваш сайт (наприклад, `https://ironproof-vbt.netlify.app`):

1. Відкрийте це посилання в браузері **Google Chrome** на вашому Android смартфоні.
2. Оскільки ми налаштували PWA, Chrome запропонує вам **"Додати на головний екран"** (Add to Home Screen) або "Встановити додаток" (Install App).
3. Погодьтесь. Тепер у вас в меню смартфона з'явиться повноцінна іконка додатка **VBT Tracker**.
4. Відкрийте додаток з іконки (він запуститься на весь екран без панелей браузера).
5. Натисніть **Start Connection** та увімкніть ваш WitMotion сенсор.
6. Android попросить дозвіл на підключення Bluetooth-пристрою – виберіть ваш сенсор у списку і почніть тренування!
